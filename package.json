{
  "name": "fakesocial-typescript-sdk",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "npx tsc --noEmit",
    "test": "bun test tests",
    "format": "npx prettier --write .",
    "lint": "npx eslint . --ext .ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
